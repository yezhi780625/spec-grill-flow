# Retro Log

<!-- 每次合併後 2 分鐘內回填一行。
     退回次數＝P3→P2 ＋ P4→P2；打回次數＝P5→P4 ＋ P5→P2；熔斷（退回 P2 達 2 次）在症狀分類記「熔斷」。
     觸發條款：本次任務中真正攔下問題或改變決定的條款（絆線、豁免、熔斷、未解事項、grill 修正、根因分析……），無則留空。
       多數攔截不會產生退回或打回——絆線觸發後免升級、豁免被駁回改寫測試、grill 追問後當場修 spec——全記在這一欄。
       這是條款日落檢視的唯一正面證據：零觸發 ≠ 零記錄,漏填會讓有用的條款被誤刪。
     經 steelman 目標確認後 PM 撤回／收斂需求、或內部提案被擋下的，也回填一行：合併日欄填攔下日、通道記「攔下」、
       觸發條款記「steelman」、備註一句理由，其餘欄留空。被攔下的需求不進流程、沒有 PR——不留這行，攔截成效就永遠不可觀測。
     繞過偵測計數規則：分子只計 feature branch 的 merge PR，排除自動化（dependabot 等）、docs-only、revert、CI 修正；
       本表一個 PR 一列（熔斷拆分後多份 spec 仍以 PR 為單位計）。差值 = 分子 − 本表新增列數（列數排除通道＝「攔下」的列，它沒有對應 PR）。
     症狀分類用 full-workflow.md 失效模式表的既有分類；新型症狀在該表新增一列並在備註標注「新增」。
     每累積 10 筆或滿一季（先到者）觸發一次檢視（繞過偵測／條款觸發紀錄／分流條件鬆緊／cycle time／grill-me 上游同步／附錄 B 條件），由回填觸發筆的人當週執行。 -->

| 合併日 | Feature | 通道 | 開工日 | 退回次數 | 打回次數 | 觸發條款 | 症狀分類 | 備註 |
|---|---|---|---|---|---|---|---|---|
| 2026-08-13 | （範例）user-auth | 完整 | 2026-08-10 | 1 | 0 | 絆線（diff 4 檔，reviewer 判免升級） | spec 有洞（P3 退回） | 範例列，填入首筆真實資料後刪除 |
